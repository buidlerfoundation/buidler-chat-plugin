type ImgLightBoxProps = {
  children: any;
  originalSrc: any;
};

const ImgLightBox = ({ children, originalSrc }: ImgLightBoxProps) => {
  // TODO: handle open full screen image with ${originalSrc}
  return children;
};

export default ImgLightBox;
